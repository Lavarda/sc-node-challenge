{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "database": "postgres",
    "username": "postgres",
    "password": "admin",
    "entities": [
        "dist/entities/**/*.{ts,js}"
    ],
    "migrations": [
        "dist/migrations/**/*.{ts,js}"
    ],
    "cli": {
        "entitiesDir": "src/entities",
        "migrationsDir": "src/migrations"
    }
}